#ifndef saveEditor

#include <string>
#include <vector>
#include <map>

void swapByteOrder(short & us);

void chargementID(std::vector<std::string> & itemID);

void chargementCoffre(std::vector<int> & la_merde_avant_le_coffre, std::map<unsigned int, unsigned int> & coffre);

#endif